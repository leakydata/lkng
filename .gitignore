target/
node_modules/
dist/
*.wasm
!published-contract/*.wasm
.env
contracts/*/target/
contracts/*/initial_state.bin
contracts/*/cell_params.bin
web/.playwright-mcp/
web/target/
.playwright-mcp/
jniLibs/
android/app/src/main/jniLibs/
android/local.properties
android/.gradle/
android/app/build/

# Release signing. The keystore and its passwords never enter the repository:
# whoever holds this key can ship a silently-accepted update to every install.
android/keystore.properties
*.jks
*.keystore

# Legacy contract snapshots are committed on purpose -- see
# contracts/legacy_contracts.toml. A migration path that requires rebuilding
# a months-old toolchain to byte-identical output is not a migration path.
!contracts/legacy/*.wasm
